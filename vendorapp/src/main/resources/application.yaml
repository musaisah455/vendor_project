server:
  port: 8083
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: vendorapp

  datasource:
    url: jdbc:postgresql://localhost:5432/vendor
    username: ENC(mqLYHw+P/hmbzOk8lO4+WE7XIhjY4KyC6UkIdd+Xc47SiBmvOosBKfBYbTYj9idI)
    password: ENC(S9BovB0WRlf1OhlknTyiPdyQwfiXsecyl62gSrnxakSjUwDeK4qZdfKUV9VQAeVh)
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      resource server:
        jwt:
          # Change this to your real issuer (Keycloak, Auth0, etc.)
          issuer-uri: http://localhost:8080/realms/master
          #issuer-uri: ${JWT_ISSUER_URI:http://localhost:8081/realms/vendor-realm}
          # Optional: If you prefer jwk-set-uri directly
          # jwk-set-uri: http://localhost:8081/realms/vendor-realm/protocol/openid-connect/certs

# Actuator (production monitoring)
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Logging
logging:
  level:
    com.example: DEBUG
    org.springframework.security: DEBUG




